[project]
name = "manim-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "click",
  "pydantic>=2",
  "PyYAML>=6",
  "typing-extensions",
]

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"
